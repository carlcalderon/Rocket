//============================================================
//
// LINKAGE
//
//============================================================

//============================================================
// DEPENDENCIES
//============================================================
var utils = require("./utils.js"),
    path  = require("path"),

//============================================================
// PATTERNS
//============================================================
REGEXP_IMPORT = function() { return /(@|\.)import\s?((url\((\S+)\))|(["'](\S+)["'])|\(['"](\S+)['"]\))/gi; };

function fixImports(content, filepath) {
    var result = content;
        extension = utils.extension(filepath).toLowerCase();

    switch (extension) {

        case 'sass' :
        case 'scss' :

            break;

    }

    return result;
}